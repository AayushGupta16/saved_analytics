from utils import display_new_user_signups_metric

def display_new_user_signups(users_df):
    """
    Displays the New User Signups metric on the dashboard.
    
    Parameters:
        users_df (pd.DataFrame): DataFrame containing user data.
    """
    display_new_user_signups_metric(users_df)
